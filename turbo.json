{
  "$schema": "https://v2-8-16.turborepo.dev/schema.json",
  // `test:coverage` is cached and infers its inputs from the package
  // directory, so it cannot see the root config the per-package
  // `vitest.config.ts` files re-export. Without this, changing a timeout
  // would be served from a stale cache.
  "globalDependencies": ["vitest.base.ts"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "build/**"],
      "inputs": [
        "src/**",
        "!src/**/*.test.ts",
        "!src/**/*.spec.ts",
        "package.json",
        "tsconfig.json",
        "tsup.config.ts"
      ],
      "env": ["NODE_ENV"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "dev:open": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": []
    },
    "lint:fix": {
      "outputs": []
    },
    "format": {
      "outputs": []
    },
    "clean": {
      "cache": false
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"],
      "cache": false
    },
    "test:watch": {
      "cache": false,
      "persistent": true
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "//#generate:schemas": {
      "dependsOn": [
        "@json-to-office/shared#build",
        "@json-to-office/shared-docx#build",
        "@json-to-office/shared-pptx#build"
      ],
      "outputs": ["schemas/**"],
      "inputs": [
        "scripts/generate-schemas.ts",
        "packages/shared/dist/**",
        "packages/shared-docx/dist/**",
        "packages/shared-pptx/dist/**"
      ]
    }
  }
}
