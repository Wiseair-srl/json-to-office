# Fonts

This page has moved. The font documentation — SAFE_FONTS, export modes,
substitution, strict validation, registering non-safe fonts, CLI flags and
warning codes — now lives in the docs site:

- <https://json-to-office.com/guide/fonts>
- In this repo: [`docs/guide/fonts.md`](guide/fonts.md)

The single source of truth for the SAFE_FONTS list is
`packages/shared/src/schemas/font-catalog.ts`.

This file is kept only so existing links (including the one in the README)
do not 404; it is excluded from the docs site build.
