---
'@json-to-office/core-docx': patch
---

The table of contents' cached entries now follow the theme's TOC styles in every reader: they no longer carry their own dot-leader tab stop, which LibreOffice and other readers that do not refresh fields showed instead of the theme's styling.
