// Common types shared between packages to avoid circular dependencies

export type ThemeName = 'minimal' | 'verizon' | 'a2a' | 'hitachi';

// All component configuration types are now defined in schemas/components.ts
// and exported from there as TypeBox-inferred types for single source of truth
